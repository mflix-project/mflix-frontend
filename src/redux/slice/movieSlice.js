/* eslint-disable */
import { createSlice } from "@reduxjs/toolkit";

let movies = createSlice({
  name: "movies",
  initialState: [],
  reducers: {},
});

export let {} = movies.actions;

export default movies;
